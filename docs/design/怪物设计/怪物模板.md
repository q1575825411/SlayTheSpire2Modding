# 怪物模板

## 怪物本体

- `怪物名`：
- `正式类型名`：
- `类型`：普通怪 / 精英 / Boss
- `所属层数`：
- `资源状态`：占位 / 已提供 / 已接入

## 数值

- `低进阶生命`：
- `高进阶生命`：
- `入场效果`：无 / <具体效果>

## 行动1

- `行动名`：
- `意图显示`：
- `低进阶数值`：
- `高进阶数值`：
- `实际效果`：
- `补充说明`：

## 行动2

- `行动名`：
- `意图显示`：
- `低进阶数值`：
- `高进阶数值`：
- `实际效果`：
- `补充说明`：

## 行动3

- `行动名`：
- `意图显示`：
- `低进阶数值`：
- `高进阶数值`：
- `实际效果`：
- `补充说明`：

## 出招规则

- `类型`：循环 / 随机 / 条件 / 混合
- `规则描述`：
- `首回合限制`：
- `连续使用限制`：

## 遭遇设计

- `遭遇名`：
- `正式类型名`：
- `房间类型`：Monster / Elite / Boss
- `弱怪池`：是 / 否
- `怪物组合`：
- `槽位要求`：默认自动 / 需要自定义遭遇场景
- `资源状态`：占位 / 已提供 / 已接入
- `结算文案`：

## 资源占位

- `资源类型`：
- `资源用途`：
- `占位文件名`：
- `最终格式`：
- `备注`：
